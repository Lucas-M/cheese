<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to 'Home', "/greetings/hello", :class => "btn btn-primary", :style=>"color:#FFFFFF;" %> 
  <%= link_to 'Cheeses', "/items", :class => "btn btn-primary", :style=>"color:#FFFFFF;" %>
  <%= link_to 'Posts', "/posts", :class => "btn btn-primary", :style=>"color:#FFFFFF;" %>
</div>

<p id="notice"><%= notice %></p>

<h1>Cheese List</h1>
  <%= link_to 'New Cheese', new_item_path, :class => "btn btn-sm btn-primary", :style=>"color:#FFFFFF;" %> 
<!-- <%= link_to 'New Item', new_item_path %>-->
<br>
<br>
<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.content %></td>
        <td><%= link_to 'Show', item %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->

    <% @items.each do |item| %>
      <span class="item">
        <p><strong><%= item.name %></strong></p>
        <p><%= item.content %></p>
        <p>
          <span><%= link_to 'Show', item %></span>
          <span><%= link_to 'Edit', edit_item_path(item) %></span>
          <span><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></span>
        </p>
      </span>
    <% end %>
